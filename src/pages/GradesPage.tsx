import React from 'react';
import Navbar from '../components/navbar/Navbar';

const GradesPage = (): JSX.Element => {
    return (
        <>
            <Navbar />
            <div className="p-grades">Grades</div>
        </>
    );
};

export default GradesPage;
